import React from "react";

import css from "../Footer/Footer.module.css";

const Footer = () => {
  return (
    <footer className={css.footer}>
      <p className={css.text}>Privacy Policy</p>
    </footer>
  );
};

export default Footer;
