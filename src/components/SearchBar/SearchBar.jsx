import React from "react";

import css from "../SearchBar/SearchBar.module.css";

const SearchBar = () => {
  return <div className={css.searchContainer}>SearchBar</div>;
};

export default SearchBar;
